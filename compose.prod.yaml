services:
  mongodb:
    image: mongo:8
    restart: unless-stopped
    volumes: ["mongodb_data:/data/db"]
  redis:
    image: redis:8-alpine
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    volumes: ["redis_data:/data"]
  api:
    image: luvio-api:latest
    restart: unless-stopped
    env_file: .env
    environment:
      PORT: "4000"
    ports: ["4002:4000"]
    depends_on: [mongodb, redis]

volumes:
  mongodb_data:
  redis_data:
